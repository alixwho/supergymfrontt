import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RutinadomingoPage } from './rutinadomingo.page';

describe('RutinadomingoPage', () => {
  let component: RutinadomingoPage;
  let fixture: ComponentFixture<RutinadomingoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RutinadomingoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
