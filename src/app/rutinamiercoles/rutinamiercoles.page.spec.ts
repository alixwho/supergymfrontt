import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RutinamiercolesPage } from './rutinamiercoles.page';

describe('RutinamiercolesPage', () => {
  let component: RutinamiercolesPage;
  let fixture: ComponentFixture<RutinamiercolesPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RutinamiercolesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
